/* =============================================================
   CONFIGURACIÓN DEL PANEL
   Este es el ÚNICO archivo que necesitas editar.
   Cambia el valor entre comillas, guarda, y listo.
   ============================================================= */

window.JMH_CONFIG = {

  // Tu ID de cliente OAuth de Google.
  // Si lo dejas vacío, el panel te guiará paso a paso para obtenerlo.
  clientId: "",

  // Horas de tu jornada. Sirve para calcular la carga del día.
  jornadaHoras: 8

};
